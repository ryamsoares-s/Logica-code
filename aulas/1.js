let nome = "Ryam"; // Nesta linha estou criando uma variavel que recebe meu nome.
let idade = 22; // Nesta linha estou criando uma variavel que recebe minha idade.

if (idade < 18) {
  console.log(nome + " é menor de idade!");
} else {
  console.log(nome + " é maior de idade!");
}
